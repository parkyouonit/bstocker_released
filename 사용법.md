# bStocker 로컬 사용법

## 1. 준비물

- Windows 10/11 또는 macOS 12 이상(Intel·Apple Silicon)
- Node.js LTS 20.19 이상
- Chrome 또는 Brave와 Rabby Wallet/MetaMask 확장 프로그램
- 본인이 관리하는 EVM 지갑, Robinhood Chain 가스용 ETH, 전략에 사용할 USDG 또는 SPCX

Safari는 브라우저 확장 지갑 호환성이 일정하지 않아 권장하지 않습니다. 이 배포본은 Cloudflare Tunnel을 사용하지 않으며 기본 주소는 이 컴퓨터의 `http://localhost:4174`입니다.

## 2. 최초 설정

### Windows

1. `1_FIRST_SETUP.bat`을 실행합니다.
2. 본인의 Rabby 또는 MetaMask EVM 지갑 주소를 입력합니다.
3. 메인넷 자동화와 무제한 Vault 위험을 이해했다면 `LIVE`를 입력합니다.

### macOS

1. `1_FIRST_SETUP.command`를 더블 클릭합니다. 처음 실행이 차단되면 파일을 Control-클릭한 뒤 `열기`를 선택합니다.
2. `권한이 없습니다`가 표시되면 Terminal에서 압축을 푼 폴더로 이동해 아래 명령을 한 번 실행합니다.

```bash
chmod +x 1_FIRST_SETUP.command 2_RUN_BSTOCKER.command 3_RECLAIM_KEEPER_FUNDS.command
```

3. 본인의 Rabby 또는 MetaMask EVM 지갑 주소와 `LIVE`를 입력합니다.

설정은 `.env.local`에 저장됩니다. 시드 문구나 사용자 지갑 개인키는 입력하지 않습니다. 주소를 잘못 입력했다면 자산·승인·자동화를 먼저 정리한 후 `.env.local`을 삭제하고 다시 설정하세요.

## 3. 실행

- Windows: `2_RUN_BSTOCKER.bat`
- macOS: `2_RUN_BSTOCKER.command`

첫 실행은 패키지 설치와 빌드 때문에 몇 분 걸릴 수 있습니다. Chrome/Brave에서 `http://localhost:4174`를 열고, 자동화 중에는 실행 창과 인터넷 연결을 유지하세요. 종료할 때는 창을 바로 닫지 말고 `Ctrl+C`를 눌러 웹·API·Keeper를 함께 종료합니다.

4174 또는 8787 포트가 사용 중이면 해당 프로그램을 먼저 종료하세요. macOS 방화벽이 Node의 수신 연결을 물으면 같은 공유기 기기에서 접속할 때만 허용하세요.

## 4. Rabby와 MetaMask 연결

`지갑 연결` 버튼을 누르면 설치된 Rabby Wallet과 MetaMask가 별도로 표시됩니다. 두 확장이 모두 설치되어 있어도 선택한 지갑만 사용합니다.

- 최초 설정에 입력한 주소와 연결 주소가 같아야 Vault 설정을 진행할 수 있습니다.
- BNB Chain LP 화면은 BSC, 5틱 화면은 Robinhood Chain으로 전환을 요청합니다.
- 네트워크 추가·전환·승인·컨트랙트 배포·입금은 선택한 지갑에서 직접 확인합니다.
- 휴대폰은 Rabby 또는 MetaMask 앱의 내장 DApp 브라우저에서 같은 공유기의 `http://컴퓨터-IP:4174`를 여세요.

## 5. 사용자별 Vault와 Keeper

각 사용자는 다음 항목을 새로 갖습니다.

- Windows DPAPI 또는 macOS 로그인 Keychain으로 보호되는 저권한 Keeper
- 자기 지갑이 owner·guardian·보상 수령자가 되는 v2.9 5틱 Vault
- 자기 컴퓨터의 `work` 폴더에 저장되는 자동화 설정과 실행 기록

macOS에서는 `.secrets/robinhood-keeper.keychain.json`에 주소와 Keychain 항목 식별자만 저장되고 실제 개인키는 로그인 Keychain에 저장됩니다. Windows에서는 `.secrets/robinhood-keeper.dpapi.json`의 암호문을 현재 Windows 사용자만 복호화할 수 있습니다.

## 6. Robinhood 5틱 자동화 시작

1. 앱 상단의 `RH 5-TICK` 화면으로 이동합니다.
2. Rabby 또는 MetaMask를 선택하고 최초 설정 주소와 같은지 확인합니다.
3. 무제한·미감사 Vault 위험 확인을 체크합니다.
4. `무제한 금고 배포`를 눌러 v2.9 Vault를 새로 배포합니다.
5. 자동화 연결 메시지에 서명합니다. 메시지 서명만으로 토큰은 이동하지 않습니다.
6. Keeper에 소량의 Robinhood Chain ETH를 보냅니다. 가스 잔액은 계속 확인해야 합니다.
7. USDG 금액을 입력하고 `승인 + 시작`을 누릅니다.
8. 지갑에서 승인 대상이 새 Vault인지, 입력한 USDG 수량과 `start` 거래 대상이 같은 Vault인지 확인합니다.

v2.9에는 온체인 금액 상한이 없습니다. 화면에 입력한 수량만 승인하지만 큰 금액의 손실을 막아주는 한도도 없습니다. 기존 v2.8 이하 Vault는 자동으로 바뀌지 않으며, 앱의 `v2.9 교체 + 새로 시작` 절차를 완료해야 새 코드가 적용됩니다.

### v2.9 가격 오라클 변경

- NAV는 Robinhood 공식 Chainlink `SPCX/USD`와 `USDG/USD` 온체인 피드를 사용하는 성과 지표입니다. 일반적인 가격 하락으로 NAV가 줄어드는 것만으로 자동 재배치를 멈추거나 USDG 종료하지 않습니다.
- v2.8의 REST bid/ask 중간값은 호가 간격이 넓을 때 SPCX 가치를 과소평가해 잘못된 종료 신호를 만들 수 있었으므로 v2.9의 손익·종료 계산에서 제거했습니다.
- 피드가 25시간보다 오래됐거나 라운드·토큰 오라클 상태가 정상이 아니면 자동 스왑하지 않습니다.
- 단, 예상 주말 휴장 구간에는 최대 72시간 동안 마지막 Chainlink, multiplier-adjusted Robinhood bid/ask 범위, DEX 30초·5분 TWAP이 모두 합의할 때 기존 포지션 재배치와 UP 수확만 계속할 수 있습니다.
- 휴장 합의 중에는 신규 시작·추가 입금이 잠기며 stale Chainlink 가격만으로 USDG 안전 종료를 실행하지 않습니다. 합의가 깨지면 즉시 `FAIL_CLOSED`로 재배치를 정지합니다.
- Chainlink 종료 조건이 맞더라도 DEX 5분 TWAP가 Chainlink보다 1.5% 넘게 낮으면 즉시 매도하지 않습니다. 이때 LP가 남아 있으면 먼저 원물로 회수해 재배치를 중단하고 다음 검증을 기다립니다.

## 7. 자동화 동작과 중지

- 가격이 5틱 범위를 벗어나고 안전가드를 통과하면 Keeper가 LP 회수 → 비율 스왑 → 새 LP 생성 → Gauge 재예치를 한 트랜잭션으로 실행합니다.
- UP은 수확할 때 Vault의 고정 수령 주소로 전달됩니다.
- 각 실행 전 전체 트랜잭션 시뮬레이션이 실패하면 실제 거래를 보내지 않습니다.
- 급락·오라클 괴리·과도한 재배치·유동성 부족 때는 정지 또는 백오프합니다.
- 모든 스왑은 온체인 TWAP 기준 최소수령량, 1% 가격 한계, 짧은 deadline을 강제합니다. Keeper는 자동 거래를 Robinhood 공식 FCFS Sequencer에 직접 제출하며 실패하면 공개 RPC로 우회 전송하지 않습니다.
- DEX와 신선한 공식 가격이 5분 약 -3% 급락을 함께 확인하고 30초 동안 지속할 때 Keeper가 LP 회수 → SPCX 전량 USDG 스왑 → 고정 owner 주소 송금을 한 트랜잭션으로 실행합니다. 허용 가격 안에서 전량을 팔 수 없으면 전체가 되돌려집니다. 배포된 v2.9의 NAV -5% 조건은 화면의 수동 안전 종료 호환용이며 자동 Keeper는 NAV 하락만으로 호출하지 않습니다.
- 공식 거래정지, 토큰/오라클 정지, 컨트랙트 검증 실패 상황에서는 위험한 시장가 스왑을 하지 않고 두 토큰 회수 상태로 전환합니다.
- 컴퓨터·실행 창·인터넷이 꺼지면 자동 재배치가 멈추며 자산은 Vault/LP 상태로 남습니다.

자동화를 끝낼 때는 앱에서 `자동화 끄기`를 먼저 실행하고 상태가 반영된 다음 `Ctrl+C`로 프로그램을 종료하세요. 필요하면 `두 토큰 회수`를 사용합니다. 수동 `안전 USDG 종료`도 동일한 온체인 -3%/-5% 조건이 확인될 때만 실행됩니다.

## 8. Keeper 지갑의 남은 ETH 회수

Keeper 개인키를 화면에 노출하거나 MetaMask로 가져오지 않고, 보호 저장소에서 직접 서명해 최초 설정 owner 주소로만 ETH를 회수할 수 있습니다.

1. 앱에서 `자동화 끄기`를 누르고 자동화가 꺼진 것을 확인합니다.
2. 실행 창에서 `Ctrl+C`를 눌러 앱·API·Keeper를 완전히 종료합니다.
3. 운영체제에 맞는 파일을 실행합니다.
   - Windows: `3_RECLAIM_KEEPER_FUNDS.bat`
   - macOS: `3_RECLAIM_KEEPER_FUNDS.command`
4. 표시된 Keeper 주소, 받는 owner 주소, 잔액, 예상 수수료와 회수액을 확인합니다.
5. 실제 전송 승인 단계에서 받는 owner 주소 전체를 다시 입력합니다.

회수 도구는 API가 실행 중이거나 로컬 자동화 설정이 아직 `armed`이면 전송을 거부합니다. 목적지는 `.env.local`에 고정된 owner 주소이며 임의 주소로 바꿀 수 없습니다. 네트워크 수수료보다 잔액이 적으면 회수하지 않습니다.

## 9. BNB Chain LP·보상·Bridge 쓰기 잠금

공개 배포본은 실제 BNB Chain 승인·스왑·민트를 기본 잠금합니다. 검토 후 `.env.local`의 아래 값을 `true`로 바꾸고 다시 빌드해야 활성화됩니다.

```text
VITE_ENABLE_MAINNET_WRITES=true
```

Pancake/Merkl 쓰기는 `VITE_ENABLE_REWARD_WRITES=true`, Bridge 쓰기는 `VITE_ENABLE_MAINNET_BRIDGE=true`가 별도로 필요합니다.

## 10. 같은 공유기의 다른 기기

실행 창에 표시되는 내부 IP로 `http://컴퓨터-IP:4174`에 접속할 수 있습니다. 같은 공유기의 신뢰하는 기기에서만 사용하세요. 인터넷 공개용 터널이나 도메인은 포함되지 않습니다.

## 11. 백업과 컴퓨터 이동

- `.env.local`: owner와 실행 허용 설정
- `work/`: Vault 주소, 실행·성과 기록
- `.secrets/`: Keeper 메타데이터 또는 DPAPI 암호문

이 파일들을 GitHub·메신저·클라우드 공개 폴더에 올리지 마세요. macOS Keychain 항목과 Windows DPAPI 키는 다른 컴퓨터로 폴더만 복사해서 사용할 수 없습니다. 새 컴퓨터에서는 새 Keeper를 만들고 기존 Vault의 Keeper 주소를 owner 지갑의 `setKeeper` 거래로 교체해야 합니다. 이전 컴퓨터를 폐기하기 전에 8장의 절차로 Keeper ETH를 회수하세요.

## 12. 중요한 위험

- 이 앱과 Vault는 감사를 받지 않았습니다.
- v2.9는 금액 상한이 없으므로 컨트랙트 오류나 시장 급변 시 손실 규모도 제한되지 않습니다.
- 5틱은 매우 좁아 재배치·가스 사용·슬리피지·MEV가 커질 수 있습니다.
- MEV 방어는 위험을 줄일 뿐 제거하지 못합니다. 공식 Sequencer는 비공개 주문 흐름이나 포함 보장을 제공하는 전용 MEV 보호 릴레이로 간주하지 않습니다.
- Keeper 잔액 부족, RPC 장애, 컴퓨터 절전은 자동화를 멈춥니다.
- 연결 지갑의 경고, 대상 주소, 승인 금액을 직접 확인하지 못했다면 서명하지 마세요.
