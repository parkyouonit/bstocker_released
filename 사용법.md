# bStocker 로컬 사용법

## 1. 준비물

- Windows 10/11 PC
- Node.js LTS 20.19 이상
- Rabby Wallet과 본인이 관리하는 EVM 지갑
- Robinhood Chain 가스용 ETH
- 5틱 전략에 사용할 USDG 또는 SPCX

이 배포본은 Cloudflare Tunnel을 사용하지 않습니다. 인터넷에 공개되지 않으며 기본 주소는 이 PC의 `http://localhost:4174`입니다.

## 2. 압축을 푼 뒤 최초 설정

1. `1_FIRST_SETUP.bat`을 실행합니다.
2. 본인의 Rabby EVM 지갑 주소를 입력합니다.
3. 메인넷 자동화 위험을 이해했다면 `LIVE`를 입력합니다.
4. 설정은 `.env.local`에 저장됩니다. 개인키나 시드 문구는 입력하거나 저장하지 않습니다.

주소를 잘못 입력했다면 `.env.local`을 삭제하고 최초 설정을 다시 실행하세요. 이미 사용 중인 Vault가 있다면 먼저 앱에서 자산과 승인을 정리해야 합니다.

## 3. 실행

1. `2_RUN_BSTOCKER.bat`을 실행합니다.
2. 첫 실행은 필요한 패키지를 설치하고 앱을 빌드하므로 몇 분 걸릴 수 있습니다.
3. 브라우저에서 `http://localhost:4174`를 엽니다.
4. 검은 실행 창은 자동화 중 계속 켜 둡니다. 종료하려면 그 창에서 `Ctrl+C`를 누릅니다.

4174 또는 8787 포트가 이미 사용 중이면 해당 프로그램을 종료한 뒤 다시 실행하세요.

## 4. 다른 사람도 자기 컨트랙트를 새로 배포하나요?

네. 각 사용자는 다음 항목을 새로 갖게 됩니다.

- 자기 Windows 계정에 암호화된 저권한 Keeper 키
- 자기 Rabby 지갑이 owner·guardian·보상 수령자가 되는 v2.6 5틱 Vault
- 자기 PC의 `work` 폴더에 저장되는 자동화 설정과 실행 기록

다른 사용자의 Vault, Keeper, 승인 또는 거래 내역은 배포본에 포함되지 않습니다.

## 5. Robinhood 5틱 자동화 시작 순서

1. 앱 상단에서 `Robinhood 5틱` 화면으로 이동합니다.
2. Rabby를 연결하고 설정 때 입력한 지갑과 같은 주소인지 확인합니다.
3. 파일럿 위험 확인을 체크합니다.
4. `제한형 금고 배포`를 눌러 자기 v2.6 Vault를 새로 배포합니다.
5. 자동화 연결 메시지에 서명합니다. 이 메시지 서명만으로 토큰은 이동하지 않습니다.
6. Keeper에 소량의 Robinhood Chain ETH를 보냅니다. 시작은 0.002 ETH 버튼을 제공하지만 사용량을 계속 확인해야 합니다.
7. 금액을 입력하고 `승인 + 시작`을 누릅니다.
8. Rabby에서 승인 대상이 방금 배포한 자기 Vault인지, USDG 수량이 맞는지, 마지막 `start` 거래 주소가 같은 Vault인지 확인합니다.

Vault 배포에는 가스가 들며, 브라우저 새로고침 때문에 배포를 반복하지 마세요. 앱의 Vault 주소와 Robinhood Chain 탐색기의 배포 거래를 먼저 확인하세요.

## 6. 자동화 동작

- 가격이 5틱 범위를 벗어나고 안전가드를 통과하면 Keeper가 기존 LP를 회수하고 비율 스왑 후 새 5틱 LP를 만듭니다.
- UP 보상은 수확 시 Vault에 고정된 사용자 수령 주소로 전달됩니다.
- 각 실행 전 전체 거래 시뮬레이션이 실패하면 실제 거래를 보내지 않습니다.
- 급락·오라클 괴리·너무 잦은 재배치·유동성 부족 때는 일시정지 또는 백오프합니다.
- PC, 실행 창, 인터넷이 꺼지면 자동 재배치는 멈춥니다. 자산이 사라지는 것은 아니며 Vault/LP 상태로 남습니다.

## 7. BNB Chain LP 기능

공개 배포본은 실제 BNB Chain 승인·스왑·민트를 기본 잠금합니다. 사용자가 직접 검토 후 `.env.local`에서 아래 값을 바꾸고 앱을 다시 빌드해야 실제 거래 버튼이 활성화됩니다.

```text
VITE_ENABLE_MAINNET_WRITES=true
```

Pancake/Merkl 보상 쓰기는 별도로 `VITE_ENABLE_REWARD_WRITES=true`가 필요합니다. Bridge 쓰기는 기본 잠금 상태입니다.

## 8. 같은 공유기의 다른 기기에서 접속

실행 창에 표시되는 이 PC의 내부 IP를 이용해 `http://PC-IP:4174`로 접속할 수 있습니다. Windows 방화벽 허용 창이 뜨면 개인 네트워크만 허용하세요. 인터넷 공개용 터널이나 도메인은 포함하지 않습니다.

휴대폰에서 Rabby 연결이 필요하면 Rabby 앱의 내장 DApp 브라우저로 위 내부 IP 주소를 여세요. PC와 휴대폰이 같은 공유기에 연결되어 있어야 합니다.

## 9. 백업과 삭제 금지 항목

- `.secrets/robinhood-keeper.dpapi.json`: 자동화 Keeper 키의 암호문입니다. 공개하거나 GitHub에 올리지 마세요.
- `.env.local`: 사용자의 owner 주소와 실행 허용 설정입니다.
- `work/`: Vault 주소, 실행 기록과 성과 기록이 들어 있습니다.

Keeper 키는 만든 Windows 사용자 계정에서만 복호화됩니다. 다른 PC에 폴더를 복사하면 새 Keeper를 만들고 기존 Vault의 Keeper를 Rabby owner 거래로 교체해야 합니다.

## 10. 중요한 위험

- 이 앱과 Vault는 감사를 받지 않았습니다.
- 5틱은 범위가 매우 좁아 재배치와 가스 사용이 잦을 수 있습니다.
- SPCX 가격 변동, 비영구손실, UP 가격 하락, 슬리피지와 MEV로 손실이 날 수 있습니다.
- Keeper 잔액이 부족하거나 RPC가 멈추면 자동화가 중단됩니다.
- Rabby 경고, 대상 주소, 승인 금액을 확인하지 못했다면 서명하지 마세요.
